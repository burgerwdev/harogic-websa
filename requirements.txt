# Runtime dependencies (installed by run.sh / the release artifact).
# Bounds are two-sided so a future major release cannot break the build silently;
# the exact versions this project is tested against are in requirements-lock.txt.
aiohttp>=3.9,<4
numpy>=1.24,<3
